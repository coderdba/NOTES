

Download the repo
Remove or rename rm Berksfile.lock - so that we avoid using old dependencies
Create .kitchen.yml with appropriate settings and attribute values
Do 'kitchen converge'
To logon to the VM - do 'kitchen logon' and sudo to root
